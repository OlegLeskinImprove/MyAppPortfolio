<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PrimaryDarkText" parent="AppTheme">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="PrimaryDarkText.18">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="PrimaryDarkButton" parent="Widget.AppCompat.Button.Borderless.Colored">
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/color_white</item>
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:width">@dimen/button_min_width</item>
        <item name="android:height">@dimen/button_height</item>
        <item name="android:padding">@dimen/button_padding</item>
    </style>
</resources>
